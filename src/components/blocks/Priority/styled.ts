import styled from 'styled-components';

const PriorityBlock = styled.div`
  position: relative;
`;

const PriorityButton = styled.button`
  background: none;
  border: none;
  cursor: pointer;
  width: 100%;
`;

export { PriorityBlock, PriorityButton };
